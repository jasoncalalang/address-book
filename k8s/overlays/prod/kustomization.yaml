apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

patches:
  - target:
      kind: Ingress
      name: address-book
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: address-book.prod.lan
