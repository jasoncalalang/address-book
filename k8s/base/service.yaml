apiVersion: v1
kind: Service
metadata:
  name: address-book
  labels:
    app: address-book
spec:
  type: ClusterIP
  selector:
    app: address-book
  ports:
    - name: http
      port: 80
      targetPort: http
